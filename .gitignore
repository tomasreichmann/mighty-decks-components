node_modules/
# Prebuilt package resources are committed for Git consumers.
output/
.export-staging/
.pnpm-store/
.agent-logs/

# The locally prepared consumer contract is committed with its source inputs.
!distribution/
!distribution/**
