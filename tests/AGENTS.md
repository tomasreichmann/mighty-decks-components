# Tests

Prefer behavioral tests. Catalog baselines are intentional release-content contracts
and require reviewed updates. Geometry and asset completeness are export contracts;
CSS classes, arbitrary DOM nesting, and mutable copy generally are not.
